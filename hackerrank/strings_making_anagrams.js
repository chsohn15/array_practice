function makeAnagram(a, b) {

    //make frequency hash for both strings
    //iterate through first string
    //check both objects to see if letter is found
    //if yes, subtract 1 from both frequency counts
    //else if letter is found but === 0, then add one to the freq count
    //add up rest of frequency counts

}

a = "cde"
b = "abc"